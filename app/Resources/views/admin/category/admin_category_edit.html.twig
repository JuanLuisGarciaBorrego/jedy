{% extends ':admin:base_admin.html.twig' %}

{% block content %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">edit</button>
    {{ form_end(form) }}

{% endblock %}
