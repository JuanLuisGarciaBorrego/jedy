{% extends ':admin:base_admin.html.twig' %}

{% block content %}
    <ul class="breadcrumb">
        <li><a href="{{ path('admin_home') }}">Home</a></li>
        <li class="active">Categories</li>
    </ul>

    {% include 'admin/_flashbag.html.twig' %}

    <div class="row margin-vertical-md">
        <div class="col-md-8">
            <h1 class="h-btn-line">Nav</h1>
        </div>
        <div class="col-md-4 text-right">
            <a href="{{ path('admin_nav_new') }}" class="btn btn-primary btn-sm">Add Nav</a>
        </div>
    </div>

    <table class="table table-striped table-hover ">
        <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for nav in navs %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ nav.name}}</td>
                <td>
                    <a href="" class="btn btn-primary btn-xs">Add content</a>
                    <a href="" class="btn btn-warning btn-xs">Edit</a>
                    <a href="" class="btn btn-info btn-xs">Translations</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
