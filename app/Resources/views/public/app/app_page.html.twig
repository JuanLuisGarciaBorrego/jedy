{% extends 'public/base_public.html.twig' %}

{% block title %} {{ page.title }} {% endblock %}

{% block content %}

    <h1>{{ page.title }}</h1>
    <p>{{ page.content }}</p>
    <hr>
    {{ translation_content(page) }}
{% endblock %}
